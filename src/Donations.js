import { useState} from 'react'
import DonationList from './DonationList'
const Doonations = (props) => {
    let {myDonorArr}=props
    const purpose=2000000
    let percentDonation
    let amountDonation
    let numDonors

  
    console.log(numDonors)
    return (<> <h2>{numDonors}</h2>
    </>  );
}
 
export default Doonations;

